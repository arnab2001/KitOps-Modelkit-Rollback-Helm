apiVersion: v1
kind: Service
metadata:
  name: {{ include "modelkit-demo.fullname" . }}
  labels:
    {{- include "modelkit-demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "modelkit-demo.selectorLabels" . | nindent 4 }}

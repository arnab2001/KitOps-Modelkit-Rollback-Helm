# Production values for modelkit-demo
# This represents the current "BAD" version that needs rollback

modelkitRef:
  model: "jozu.ml/arnabchat2001/modelkit-demo:v3"  # BAD model version (current working)
  code: "jozu.ml/arnabchat2001/modelkit-demo:v3"   # Working code version

# Production settings
replicaCount: 2

# Production resource limits
resources:
  limits:
    cpu: 2000m
    memory: 4Gi
  requests:
    cpu: 1000m
    memory: 2Gi

# Production environment
env:
  MODEL_PATH: "/model"
  ENVIRONMENT: "production"
